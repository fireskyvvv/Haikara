#toggle--build-on-build-process .unity-label {
    white-space: nowrap;
    flex-grow: 1;
    flex-shrink: 0;
}

#toggle--build-on-build-process .unity-toggle__input {
    flex-direction: row;
    padding-left: 16px;
}
